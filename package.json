{
  "name": "link-control-sales-os",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp index.html public/index.html && echo static-build"
  },
  "dependencies": {}
}
